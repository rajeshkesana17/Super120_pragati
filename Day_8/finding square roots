def binary(n):
    l,r,ans=1,n,0
    while l<=r:
        mid=(l+r)//2
        if mid*mid>=n:
            ans=mid
            r=mid-1
        else:
            l=mid+1
    return ans
n=25
res=binary(n)
print(res)
